#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>
#include <math.h>
#include <vector>
using namespace std;
// ........................................ROTATE BY ONE.....................................................
/* void rotate(int* a, int n) {

   int temp = a[0];
   for (int i = 1; i < n; i++) {
       swap(a[i], a[i - 1]);
   }
   a[n - 1] = temp;

}                                         //12345
int main() {

   int n;
   cin >> n;
   int arr[n];
   for (int i = 0; i < n; i++) {
       cin >> arr[i];
   }
   rotate(arr, n);
   for (int i = 0; i < n; i++) {
       cout << arr[i];
   }
}
 */
